import { GoogleNoticeSheet } from "@/components/google-notice-sheet";

// Google マップの法的通知（native formSheet ルート）。このアプリについて画面
// からのドリルイン（router.push）。
export default function GoogleNoticeRoute() {
  return <GoogleNoticeSheet />;
}
