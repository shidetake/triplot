// iOS 26 の NativeTabs（Liquid Glass の浮島タブバー）は RN の flex レイアウトの
// 外（ネイティブの合成レイヤー）に浮くため、RN 側からは高さを取得できない。
// 上端は画面下端から実測 約83pt（複数画面で確認済み）。FAB・トースト等、
// タブバーの上に確実に出したい浮遊要素はここから逃がす。
export const MOBILE_TAB_BAR_TOP = 83;

// 一覧の行の左右の余白。**この値は器ではなく行に当てる。**
//
// 行が背景や区切り線を持つ一覧では、余白を器に持たせると線と面が内側で止まる。
// iOS の UITableViewCell も常に幅いっぱいで、中身だけ内側に寄せる作り。web の
// 一覧も li 側に padding を置くのが定型（ul に置くと border-bottom が内側で
// 止まる）。規約は docs/ui-guidelines.md「行にするかカードにするか」。
//
// スワイプで消せる一覧では、これを守らないと**行が縮んだ時に中身が角丸の縁に
// 張り付く**（実機フィードバック: 場所の「位置を設定」が縁の隣に来て詰まった）。
//
// 地色も区切り線も持たないもの（フォーム等）は器にまとめて付けてよい。
export const LIST_ROW_PADDING_H = 16;
