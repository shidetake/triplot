# triplot 残件

このファイルは機能残件の覚え書き。完了したら該当行を消す（`[x]` のまま残さない）。

## 残件

### 14. LP 本体（コピー/動画/スクショ）
骨組み（ルート・共有ヘッダー・URL/IA）は実装済み。LP のコンテンツ制作が残。

### 15. OAuth 同意画面のロゴ設定（ブランド確認）
カレンダーエクスポートの本番公開は完了（2026-07-14。スコープを非 sensitive の
`calendar.app.created` に絞り、`/privacy` 作成・ドメイン確認のうえ公開切替。
一般アカウントでのエクスポート動作確認済み）。残っているのはロゴのみ:
アップロードするとブランド確認（審査・数日）が発動するため未設定にしてある。
同意画面にロゴを出したくなったら設定して審査を通す。
（iOS のカレンダーエクスポートは 2026-07-14 実装済み）

### 16. ログイン方法の追加（Google と Apple を1つのアカウントで使う）
Google で使っている人が Apple でも（逆も）入れるようにする。設定に「Apple を
追加」「Google を追加」を置き、今のアカウントにログイン方法を足す。
- **足すだけにする。アカウントの統合（中身の移し替え）はしない。** その
  Google / Apple で既に別のアカウントがある場合は、追加を断って案内する:
  「この Apple は別のアカウントで使われています。そのアカウントが不要なら、
  Apple でログインしてアカウントを削除してから、もう一度追加してください」。
  うっかりできた空のアカウントはこれで自力で直せる。両方に中身がある人は
  個別対応し、同じ相談が続くようなら統合を検討する（その時はゲストの
  引換券方式を普通のアカウントに広げる。追加はそのまま土台に使える）。
- iOS でも作れる。Supabase のログイン部品（2.110.0）は、Google / Apple から
  受け取った本人確認の証明でログイン方法を足す形に対応している（型で確認。
  実際の動作は未確認なので、最初に確かめる）。ゲスト切り替えの migration の
  コメントにある「iOS と噛み合わない」は、今の版では当てはまらない。
- 前提: Supabase の管理画面で「手動での連携を有効にする」をオンにする
  （本番・staging とも）。
- ゲストの切り替えは今の引換券方式のまま（既にアカウントを持つ人がゲストで
  参加した場合に、中身を移して片付けられるのはこちらだけのため）。
