# triplot 残件

このファイルは機能残件の覚え書き。完了したら該当行を消す（`[x]` のまま残さない）。

## 残件

### 12. iOS アプリ公開
実装（Expo/React Native、`apps/mobile`）は完了済み。残っているのは
App Store 公開（TestFlight から本番リリースへ）。

提出前にやること:

- [ ] **AI Gateway のクレジットを買う**。無料枠は月あたりヘビーユーザー2人分
  程度しかなく、公開して人が増えると取り込みが止まる。枠と上限の詳細は
  [docs/design/import-flow.md](docs/design/import-flow.md) の
  「AI Gateway の無料枠の制限」。
  **公開前に既に効いている**（2026-08-27）: 開発者1人の利用で 78 通が
  `rate_limit` で失敗し、取り込みが止まった。行はリトライ待ちのまま残るので
  データは失われないが、クレジットが入るまで進まない。
- [ ] **バージョンを 1.0.0 にする**。`apps/mobile/app.config.ts` の `version`
  （ビルド番号は `eas.json` の `autoIncrement` が自動で上げるので触らない）。
  **提出用ビルドの直前に上げる** — TestFlight の確認ビルドを 1.0.0 で
  埋めてしまわないため。手順は [docs/versioning.md](docs/versioning.md)。
- [ ] **審査員用のデモアカウントを審査メモに書く**。入口が OAuth（Google /
  Apple）だけなので、審査員がサインインできないと即リジェクトされる。
  App Store Connect のバージョン情報 → 「App Review Information」に
  メール／パスワードを記載する。開発用ログインは `next dev` と EAS の
  preview ビルドにしか入っていない（本番ビルドにはボタン自体が無い）ので、
  **審査用のアカウントを別途用意して渡す**必要がある。
- [ ] **migration をスカッシュする**。「本番運用フェーズに入った」と宣言する
  タイミングと同じ。以降は backfill を真面目に書く運用に切り替わる
  （AGENTS.md「Migration ポリシー（開発期間中）」）。スキーマが固まって
  からでよい。

### 13. アンドゥ（取り消す）の仕組み
削除したあとにトーストで「取り消す」を出せるようにする。今は誤操作から守る
手段が確認ダイアログしか無いので、**安い削除にも確認が必要**になっている。

アンドゥができると2つ変わる:

- 復旧コストの安い削除（TODO 等）で、確認そのものを省ける場所が増える
- TODO の一覧で**フルスワイプ削除**を解禁できる（一手で消える仕草は、
  取り消せる前提でしか成立しない）

考え方は ui-guidelines の「確認の要否は復旧コストで決める」。実装は「消してから
一定時間だけ元に戻せる」形（論理削除にせず、トーストが消えるまで再 insert 用の
データをクライアントに持つ、等）を検討する。

### 14. LP 本体（コピー/動画/スクショ）
骨組み（ルート・共有ヘッダー・URL/IA）は実装済み。LP のコンテンツ制作が残。

### 15. OAuth 同意画面のロゴ設定（ブランド確認）
カレンダーエクスポートの本番公開は完了（2026-07-14。スコープを非 sensitive の
`calendar.app.created` に絞り、`/privacy` 作成・ドメイン確認のうえ公開切替。
一般アカウントでのエクスポート動作確認済み）。残っているのはロゴのみ:
アップロードするとブランド確認（審査・数日）が発動するため未設定にしてある。
同意画面にロゴを出したくなったら設定して審査を通す。
（iOS のカレンダーエクスポートは 2026-07-14 実装済み）

### 17. iOS のピッカー2つを react-native-screens の formSheet に寄せる
通貨選択とコピー元選択だけ RN core の `<Modal presentationStyle="pageSheet">`
を使っていて、他のシート（react-native-screens の formSheet）と API が違う。
この API は detent を持てないので**中身が短くても高さが縮まない**（コピー元が
1件でも画面いっぱいに出る）。グラバー・見出しの見た目は `PageSheet`
（`components/page-sheet.tsx`）で揃えてあるが、高さだけは揃えられない。

理由は「formSheet の中にさらに `ScreenStack` を入れ子にすると元の画面と
二重露光のように重なる」という実機で確認した不具合。ただし**これは当時この
リポジトリで試して出した結論で、上流に既知の issue として報告されているかは
未確認**。使い方の問題で書き方次第では動く可能性も残っている。

- 現状 `react-native-screens` 4.26.2（`~4.26.0` 指定）。最新の安定版 4.27.0
  （2026-08-07）のリリースノートに該当する修正は無い（formSheet 関連は
  Android の1件のみ）。上げても変わらない。
- `5.0.0-alpha` で Stack v5 が進行中なので、安定したら試し直す価値がある。
- 上流に issue を出して確認するのも手。

寄せられれば ui-guidelines の「RN のシート」の例外が1つ消える。

### 18. 別行動する旅行（年表を人ごとにする）

途中で合流・離脱する旅行に対応する。**いまのモデルは「旅行＝1つの身体」**で、
時差移動を並べた年表が旅行に1本しかない。ある瞬間に旅行は1つの時間帯にいる、
という前提で全部が組まれている。

実例（2026-09 時点で予定されている旅行）: 1人はアメリカ経由でペルー、帰りに
西海岸に寄る。そこへ別の友達が行き、さらに遅れてもう1人が行く。後から来た人が
先に帰国し、残りは数日後に帰る。**同じ瞬間に3人が3つの時間帯にいる。**

壊れるのは年表を使っている機能ぜんぶ:

| 機能 | 壊れ方 |
|---|---|
| 通常の予定・費用の実効タイムゾーン | 誰の年表で解決するのか決まらない |
| 移動日の「出発側／到着側」の2択 | 同時に複数の移動があると2択にならない |
| 費用の並び（壁時計→絶対時刻） | 別の脚の人の支払いが数時間〜1日ずれる |
| 取り込みの日時の現地化 | 「その瞬間、旅行はどこに」が定義できない |
| カレンダーの列 | 1日1タイムゾーン、移動日だけ2列、が前提 |
| カレンダーへの書き出し | 同上 |

#### モデル: 年表を人ごとに導出する（新しいテーブルは作らない）

時差移動の予定は**既に参加者を持っている**（`participants_everyone` と
`event_participants`）。**その人が参加している時差移動だけを並べれば、その人の
年表**になる。事実は1箇所（移動の参加者）に置き、年表は毎回導出する
（[docs/design/timezone.md](docs/design/timezone.md) の「持たせず毎回導出」と
同じ考え方）。

**全員参加の移動しかない旅行では全員の年表が同じ＝いまと挙動が変わらない。**
移行は無風で、別行動は「その移動の参加者を絞る」だけで表現できる。

誰の年表を使うかを決めるのが本体:

| 対象 | 使う年表 |
|---|---|
| 予定 | その予定の参加者（全員参加なら今と同じ） |
| 費用 | **支払った人**（どこで払ったかは払った人の居場所） |
| 取り込みの現地化 | **転送した本人＝自分**（レシートは自分の買い物） |
| カレンダーの列 | **見ている人**（既定は自分） |

取り込みは**今より正しくなる**。いまは旅行の年表を使っていて、それが自分と
一致するのは偶然でしかない。「見る人によって答えが変わってよい値」という点で、
予定の色が閲覧者視点で変わるのと同じ性質。

**移動日の選択（`tz_disambig_transit_id` / `tz_disambig_side`）はそのまま使える。**
「どの移動の、どちら側か」を指しているので年表が複数でも意味が変わらない。
候補が必ず2つではなくなるだけで、**スキーマは触らない**。

#### 「合流」という状態は持たない

**分かれたか**は持たない。人ごとの年表を比べれば分かる（状態として持つと
参加者と二重持ちになりズレる）。

**合流したか**も概念として持たない。**同じタイムゾーン ≠ 同じ場所**
（ホノルルとタヒチは同じ UTC−10、東京とソウルは同じ +9）で、判定の根拠に
ならない。そしてアプリはこの概念を必要としない——要るのは「誰がどの移動に
乗ったか」だけ。

ただし**既定値の提案には使ってよい**。新しい予定の参加者の初期選択を「その
時刻に同じ時間帯にいる人」にするのは、たいてい当たるし外れても直せる。
**推測は既定値に使ってよく、事実の判定に使ってはいけない**（移動日の
タイムゾーンを経度や時刻で推測して初期選択にしているのと同じ線引き）。

#### 付け忘れを検出して知らせる

参加者の付け忘れは**静かに間違う**（誰かの年表が勝手に別の土地に飛ぶ）ので、
気づける方が価値がある。強い順に3つ:

1. **年表が繋がっていない** — ある人の連続する移動で、前の到着地の時間帯と
   次の出発地の時間帯が違う。純粋に構造の話で誤検出が無い。付け忘れはほぼ
   これに落ちる。
2. **同じ予定に、その時刻に違う時間帯にいる人が入っている** — 「この夕食は
   3人だが、Aさんはこの時ペルーにいる」。1では捕まらない「全員参加のままに
   した予定」がここで出る。
3. **場所の座標と年表が食い違う** — 年表の外から矛盾を指摘できる唯一の
   手がかり。ただし**店名由来の場所は当てにならない**（実データ: Uber の費用
   3件が「Uber 社の東京の拠点」に解決され、経度が全部 139.74 だった。実際は
   2件がホノルル）。使うなら地図に位置を確定した場所に限る。

出し方は該当箇所の近くにインラインで amber の警告（ui-guidelines「エラー・
警告はトーストではなく該当箇所の近くに」「進めるが知っておくべき」= amber）。
旅行の設定に「確認が必要 (N)」の一覧があると探しに行ける。**止めない**——
意図的にそうしている場合もあるので知らせるだけにして、判断はユーザーに残す。
**保存しない**（直した後も警告が残る／直っていないのに消えるを避ける）。

#### 表示

**既定は自分の年表で描く。** 他人の予定は絶対時刻を自分の壁時計に直して置く。
全部同じ物差しに乗るので比較はできるが、ペルーにいる人の「19:00 の夕食」が
自分の時間で出るので、その人の一日を計画する用途には向かない。

なので**「誰の時間で見るか」の切り替え**を足し、**年表が分岐している期間だけ
出す**（移動日のタイムゾーン選択を移動日にだけ出すのと同じ作法）。分岐して
いる日は、いま「日本 → ハワイ」を出している位置に誰の時間帯かを添える。

同時に見たい場合は、**既にある移動日の2列分割の一般化**で表現できる（あれは
「同じ日に2つの時間帯が並ぶ」表現そのもの）。ただし列が増えるほど狭くなるので
**既定は切り替え、並置は後回し**。

#### 段階

1. ~~**年表の導出を人ごとにする**（内部だけ）。誰の年表を使うかを上の表どおりに
   固定する。全員参加の旅行では挙動が変わらないので、ここだけ先に入れられる。~~
   **済（2026-09-12）。** `timelineFor` / `timelineForEvent`（`schedule.ts`）で
   絞り、予定・費用・取り込み・場所の並び・書き出し・地理バイアス・両フォームに
   通してある。設計は [docs/design/timezone.md](docs/design/timezone.md) の 3.6 節。
2. ~~**付け忘れの検出**（1と2の判定）。~~ **済（2026-09-12）。** `timelineIssues.ts`。
   予定・移動のフォームにインライン、旅行の設定に「確認が必要 (N)」。3（座標との
   食い違い）は入れていない。
3. **カレンダーを自分の年表で描く**＋分岐期間だけ切り替えを出す。いまは列を
   旅行全体の移動から組み、予定の TZ の列がその日に無ければ日付一致の列に置いて
   いる（`colFor` の保険）。
4. **並置**（本当に要ると分かってから）。

#### 注意

- コストは「広く浅い」。年表を受け取る関数が予定・費用・場所の並び・書き出しと
  広範囲にあり、全部に「誰の」を通す必要がある。**通し忘れても今と同じ挙動に
  なるだけ**なので壊れ方が静かで、テストで縛らないと見落とす。
- 合流をまたぐ移動を「全員」のままにすると、まだ合流していない人の年表にも
  入る。上の検出1がこれを捕まえるので、**検出を先に入れてから**表示に進むと
  安全。
